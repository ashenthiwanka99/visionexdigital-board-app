
export enum LaneId {
  Todo = "todo",
  InProgress = "in-progress",
  Approved = "approved",
  Rejected = "rejected",
}

export enum Priority {
  Low = "Low",
  Medium = "Medium",
  High = "High",
}

export type FooterKind = "links" | "comments" | "date" | "reports" | "stream";

